DECLARE fecha DATE DEFAULT CURRENT_DATE("America/Bogota");
DECLARE fechamax DATE DEFAULT DATE((SELECT MAX(FECHA_REQUERIDA) FROM LOGISTICA.PEDIDOS_vs_DESPACHOS_FK));

SELECT fechamax;
SELECT fecha;
SELECT EXTRACT(DAYOFWEEK FROM CURRENT_DATE("America/Bogota"));

IF (EXTRACT(DAYOFWEEK FROM CURRENT_DATE("America/Bogota")) > 1) THEN

    IF fechamax < fecha THEN
        SELECT CONCAT('NO CARGO DATO NISE DE HOY  ', CAST(FORMAT_DATE("%Y/%m/%d", CURRENT_DATE("America/Bogota")) AS STRING));
    END IF;

END IF;