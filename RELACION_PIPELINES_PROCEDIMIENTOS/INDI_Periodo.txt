/*DECLARE periodop INT64 DEFAULT CAST(FORMAT_DATE("1%y%m",CURRENT_DATE("America/Bogota")) AS INT64);

TRUNCATE TABLE COMERCIAL.PERIODO_DIASH;
INSERT INTO COMERCIAL.PERIODO_DIASH
SELECT * FROM COMERCIAL.PERIODO_DIASH ORDER BY PERIODO;

DELETE MAESTRO.TABLA_PROYECCION
WHERE PERIODO=periodop;

INSERT INTO MAESTRO.TABLA_PROYECCION
SELECT * FROM MAESTRO.TABLA_PROYECCION
WHERE PERIODO=periodop;*/
